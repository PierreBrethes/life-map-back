# LifeMap Backend

Backend API for LifeMap - a gamified 3D dashboard for managing your life (finances, health, social, etc.)

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Run

```bash
uvicorn app.main:app --reload
```
